class AddCommandTests extends BaseCommandTests {
// nadir,the low point of the suffering of someone,,0
  test("add new word") {
    implicit val addStorage = makeStorage("test_add_words.csv", "practice_sessions_read_only.csv")
    // val expected = 
  }
}
